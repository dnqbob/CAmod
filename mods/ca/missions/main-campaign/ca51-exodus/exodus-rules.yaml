^Palettes:
	PaletteFromFile@terrain-temperat:
		Filename: alien.pal
	TintPostProcessEffect:
		Red: 1.3
		Green: 1
		Blue: 1.5
		Ambient: 0.5

^BaseWorld:
	TerrainLighting:

World:
	LuaScript:
		Scripts: campaign.lua, exodus.lua
	MissionData:
		Briefing: We have reached the gateway. GDI have a strong presence surrounding it, but are currently in the process of evacuating.\n\nThe bulk of our forces are holding back the Overlord and Soviets from the north, your goal is to use what forces we can spare to secure the gateway.\n\nWe have also detected Malefic forces amassing nearby, so you have a choice to make.\n\nYou can attempt to hold the Malefic forces back long enough for GDI to retreat through the gateway, or you can attempt to fight your way through GDI to take control of the gateway by force.\n\nOur Watchers have infiltrated the GDI base to observe their evacuation. Analyse the situation and use your best judgement.
	MapOptions:
		ShortGameCheckboxEnabled: False
	MusicPlaylist:
		StartingMusic: fwp

Player:
	PlayerResources:
		DefaultCash: 6000

VoidSpikeSpawner:
	Inherits: ^WeaponDetonaterDummy
	PeriodicExplosion:
		Weapon: VoidspikeSpawner
	EditorOnlyTooltip:
		Name: Voidspike Spawner

VoidCannonTarget:
	Inherits: ^InvisibleDummy
	Targetable:
		TargetTypes: VoidCannonTarget
	Immobile:
		OccupiesSpace: false
	Health:
		HP: 1
	HitShape:
	BodyOrientation:
		QuantizedFacings: 1

^EmpMissilePower:
	AttackOrderPowerCA@EMPMISSILE:
		DisplayTimerRelationships: Ally

^IonCannonPower:
	DetonateWeaponPower@IonStorm:
		DisplayTimerRelationships: Ally

^ColonySpikePower:
	DetonateWeaponPower@ColonySpike:
		Prerequisites: ~disabled

colonyspike.provider:
	-UpdatesSupportPowerTimer:

HOSP:
	PeriodicProducerCA@MEDIC:
		RequiresCondition: !is-neutral && !scrinplayer && !owned-by-ai
	PeriodicProducerCA@REJUVENATOR:
		RequiresCondition: !is-neutral && scrinplayer && !owned-by-ai

MACS:
	PeriodicProducerCA@MECHANIC:
		RequiresCondition: !is-neutral && !scrinplayer && !owned-by-ai
	PeriodicProducerCA@ARTIFICER:
		RequiresCondition: !is-neutral && scrinplayer && !owned-by-ai

HQ:
	ExternalCondition@PowerDown:
		Condition: powerdown

EYE:
	ExternalCondition@PowerDown:
		Condition: powerdown

# Enable subfaction specific tech

STCR:
	Buildable:
		Prerequisites: anyradar, ~vehicles.scrin

LCHR:
	Buildable:
		Prerequisites: anyradar, ~vehicles.scrin

OBLT:
	Buildable:
		Prerequisites: scrt, ~vehicles.scrin

ATMZ:
	Buildable:
		Prerequisites: scrt, ~vehicles.scrin

ENRV:
	Buildable:
		Prerequisites: scrt, ~aircraft.scrin

advart.upgrade:
	Buildable:
		Prerequisites: anyradar, ~player.scrin

ioncon.upgrade:
	Buildable:
		Prerequisites: scrt, ~player.scrin

coalescence.upgrade:
	Buildable:
		Prerequisites: scrt, ~player.scrin

# Hunt() requires only 1 AttackBase
DEVA:
	-AttackFrontalCharged:
	-Armament@PRIMARYUPG:
	-AmbientSound@CHARGE:
	-WithIdleOverlay@CHARGE1:
	-WithIdleOverlay@CHARGE2:
	-WithIdleOverlay@CHARGE3:
